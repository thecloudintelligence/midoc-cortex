VITE_AGENT_API_URL=http://localhost:8080
VITE_USE_MOCK=false

# Fallback if sign-in response omits scope (must match midoc-agents .env)
# VITE_MIDOC_DOCTOR_ID=
# VITE_MIDOC_HOSPITAL_ID=
# VITE_MIDOC_BRANCH_ID=
